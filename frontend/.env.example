# Local dev: leave unset — Vite's dev-server proxy sends /api to localhost:8000.
# Production (Vercel): set this to your deployed Render backend + /api, e.g.
# VITE_API_URL=https://medscribe-backend.onrender.com/api
VITE_API_URL=
